import './bootstrap';

import Alpine from 'alpinejs';

window.Alpine = Alpine;

Alpine.start();

// window.Echo.channel('room-allocation')
//     .listen('.allocation.done', (e) => {
//         alert(e.message); // or toastr.success(e.message);
//     });

